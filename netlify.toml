[build]
  # No build step; publish the repository root
  command = ""
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Route /download/:name -> function
[[redirects]]
  from = "/assets/:name"
  to = "/.netlify/functions/assets"
  status = 200